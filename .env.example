# NesVentory Environment Configuration
# Copy this file to .env and update with your actual values
# IMPORTANT: Never commit .env to version control!

# =============================================================================
# SECURITY SETTINGS (REQUIRED)
# =============================================================================
# Generate a secure secret key using: python -c "import secrets; print(secrets.token_urlsafe(32))"
# DO NOT use the example values below in production!
SECRET_KEY=your-super-secret-key-change-this-in-production
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production

# JWT token expiration in minutes
# Default in config.py is 1440 (24 hours) for convenience
# For production, consider a shorter duration (e.g., 30-60 minutes) for better security
ACCESS_TOKEN_EXPIRE_MINUTES=30

# JWT algorithm (recommended: HS256)
JWT_ALGORITHM=HS256

# =============================================================================
# DATABASE SETTINGS (REQUIRED)
# =============================================================================
DB_HOST=nesventory_db
DB_PORT=5432
DB_USER=nesventory
# Generate a strong password for production!
DB_PASSWORD=change-this-database-password
DB_NAME=nesventory

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
PROJECT_NAME=NesVentory
BACKEND_PORT=8001
FRONTEND_PORT=5173

# =============================================================================
# CORS SETTINGS
# =============================================================================
# Comma-separated list of allowed CORS origins
# For development: include both localhost and 127.0.0.1 with your frontend port
# For production: use your actual frontend domain(s)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
